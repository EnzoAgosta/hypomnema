[pytest]
addopts = -m "not hypothesis"
markers =
  unit: unit tests
  roundtrip: roundtrip tests
  backend: backend behavior tests
  policy: policy behavior tests
  helpers: helper API tests
  hypothesis: hypothesis-based tests
